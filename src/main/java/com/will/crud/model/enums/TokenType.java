package com.will.crud.model.enums;

/**
 * Enumeración que representa el tipo de token utilizado en la autenticación.
 * En este caso, el tipo de token es "BEARER".
 */
public enum TokenType {
  BEARER
}
